{
  "servers": {
    "dd": {
      "type": "stdio",
      "command": "pwsh",
      "args": ["-NoProfile", "-NonInteractive", "-File", "${workspaceFolder}/.dd/mcp/server.ps1", "-Root", "${workspaceFolder}"]
    }
  }
}